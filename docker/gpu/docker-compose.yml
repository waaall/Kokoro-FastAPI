name: kokoro-fastapi-zh-gpu
services:
  kokoro-tts:
    image: kokoro-fastapi-zh-gpu
    container_name: kokoro-fastapi-zh-gpu
    build:
      context: ../..
      dockerfile: docker/gpu/Dockerfile
    volumes:
      - ../../api:/app/api
      - ./models:/app/api/src/models
      - ./voices:/app/api/src/voices
    user: "1000:1000"  # 与 Dockerfile 中 appuser 的 UID/GID 保持一致
    ports:
      - "8880:8880"
    environment:
      - PYTHONPATH=/app:/app/api
      - USE_GPU=true
      - PYTHONUNBUFFERED=1
      - REPO_ID=hexgrad/Kokoro-82M-v1.1-zh
      - DEFAULT_VOICE=zf_094
      - KOKORO_V1_FILE=v1_1_zh/kokoro-v1_1-zh.pth
      - VOICES_DIR=/app/api/src/voices/v1_1_zh
      - API_LOG_LEVEL=DEBUG
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # # Gradio UI service
  # gradio-ui:
  #   image: ghcr.io/remsky/kokoro-fastapi-ui:v${VERSION}
  #   # Uncomment below to build from source instead of using the released image
  #   # build:
  #     # context: ../../ui
  #   ports:
  #     - "7860:7860"
  #   volumes:
  #     - ../../ui/data:/app/ui/data
  #     - ../../ui/app.py:/app/app.py  # Mount app.py for hot reload
  #   environment:
  #     - GRADIO_WATCH=1  # Enable hot reloading
  #     - PYTHONUNBUFFERED=1  # Ensure Python output is not buffered
  #     - DISABLE_LOCAL_SAVING=false  # Set to 'true' to disable local saving and hide file view
  #     - API_HOST=kokoro-tts  # Set TTS service URL
  #     - API_PORT=8880  # Set TTS service PORT
